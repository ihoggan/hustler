# HUSTLER dependencies — these two, and nothing else.
#
# No numpy, no asset files, no image or sound libraries. Everything the game
# draws or plays is synthesised in code at runtime. This is a deliberate
# constraint; adding to this file needs an explicit decision.

pygame==2.6.1
pymunk==7.3.0
